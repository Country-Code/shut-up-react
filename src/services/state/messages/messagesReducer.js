import * as actionTypes from "./actionTypes";

export default (state = {}, action) => {
    let newState = {...state};
    switch (action.type) {

        default:
            break;
    }
    return newState;
};
